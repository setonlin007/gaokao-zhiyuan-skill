# _selfcheck_data/ — 自测合成数据（非真实！）

> ⚠️ **本目录全部为合成样例数据，`source_url` 一律为 `SAMPLE`。**
> 仅供 `../selfcheck.py` 验证 lib 确定性逻辑，**绝不可当真实高考数据使用**（守 DESIGN 铁律一）。
> 真实数据建库见 `../../data/`，来源必须是可回溯的官方/权威链接。

包含一个合成省份 `湖北/`（院校专业组粒度），用于跑通：
P0 阶段判定 → 位次换算 → 选科硬过滤 → 冲稳保多年均值分档 → 招生计划修正 → 梯度配额 → 调剂提示。
